import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

   @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(
      //   title: const Text('App tenis', style: TextStyle(fontWeight: FontWeight.bold, color: Colors.black54),),
      //   backgroundColor: const Color(0XFF5cd9ad),
      // ),
      body: Stack(children: [

      ]),
    );
  }
}